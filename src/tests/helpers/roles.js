const faker = require('faker');

export const superAdminRole = {
  title: faker.name.title()
};
export const adminRole = {
  title: faker.name.title()
};
export const userRole = {
  title: faker.name.title()
};
export const roleIdTest = {
  title: faker.name.title()
};
export const testRole = {
  title: "Test-User"
};
export const updateTestRole = {
  title: "Test User"
};